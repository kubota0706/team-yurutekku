1. **事前準備（必要なツールのインストール）**
   - [Node.js](https://nodejs.org/)（LTSバージョン推奨）
   - [Git](https://git-scm.com/)
   - スマホで動作確認する場合は、スマートフォンに **Expo Go** アプリ（iOS / Android）をインストール

2. **リポジトリのクローン**
   ```bash
   git clone https://github.com/kubota0706/team-yurutekku
   ```

3. **依存パッケージのインストール**
   `react-native` フォルダに移動し、必要なパッケージをインストールします。
   ```bash
   cd team-yurutekku/react-native
   npm install expo@~54.0.0
   ```

4. **Firebaseの設定**
   FirebaseコンソールでFirestoreおよびCloud Storageを有効化し、取得した設定情報（API Key等）を記述した設定ファイル（例: `firebaseConfig.js`）を `react-native/src/dao/` 直下に配置します。

5. **アプリの起動**
   ```bash
   npx expo start
   ```
   ターミナルに表示されるQRコードを **Expo Go** アプリで読み取って実行します。